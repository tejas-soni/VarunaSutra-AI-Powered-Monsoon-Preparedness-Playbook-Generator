{
  "_comment": "Merge these 'scripts' into your Vite package.json. 'verify' is the quality gate — must pass before EVERY submission and after EVERY Gemini edit.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:e2e": "playwright test",
    "lint": "eslint . --ext ts,tsx --max-warnings 0",
    "lint:fix": "eslint . --ext ts,tsx --fix",
    "format": "prettier --write \"src/**/*.{ts,tsx,css}\"",
    "audit:complexity": "node audit.mjs",
    "verify": "npm run lint && npm run typecheck && npm run test:cov && npm run build",
    "check": "npm run verify"
  }
}

